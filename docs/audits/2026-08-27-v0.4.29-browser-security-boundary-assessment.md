# OpenBot v0.4.29 browser-security-boundary assessment

Date: 2026-08-27  
Implementation commit: pending release commit

## Outcome

OpenBot v0.4.29 is a documentation-hardening patch for the v0.4.28 browser research-host capability. The public security boundary now explains loopback defaults, exact host configuration, URL credential rejection, disabled redirects, approval behavior, untrusted fetched content, and operator responsibility for DNS and network configuration.

## Verification

- `git diff --check`: passed.
- `npm run check`: passed, 137/137 release checks.
- The v0.4.28 implementation security review found 0 findings; this patch changes only version metadata, changelog, assessment documentation, and `SECURITY.md`.

## Product and security assessment

The patch improves operator setup and threat-model clarity without changing runtime behavior, dependencies, permissions, or data flow. It adds no account, hosted service, GPU requirement, or mandatory spend. The v0.4.28 exact-host allowlist, loopback default, approval gate, redirect restriction, workspace containment, cancellation, redaction, and LAN authentication remain the authoritative runtime controls.

## Remaining gaps

DNS-rebinding defenses, authenticated browser profiles, JavaScript-rendered pages, isolated browser profiles, connectors, desktop automation, signed extensions, collaboration, multi-user authentication, remote workers, and full-duplex clients remain future work.
