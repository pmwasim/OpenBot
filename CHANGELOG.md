# Changelog

## Unreleased

- Added bounded local file, shell, and browser workers with workspace and network boundaries.
- Added recoverable task execution, action-bound approvals, one-time approval consumption, SSE events, and redacted audit export.
- Added CLI execution/export parity and a local operator console with responsive task, approval, health, and event views.
- Added Ubuntu user install, desktop launcher, zero-spend configuration example, and recoverable uninstall helper.
